<td>
	<div class="tooltip">
		<img class="{{alignment}} character" data-name="{{name}}" data-alignment="{{alignment}}" data-rank="{{rank}}" data-faction="{{#each faction}} {{this}} {{/each}}" src="{{pic}}" />
		<span class="tooltiptext">{{name}}</span>
	</div>
</td>